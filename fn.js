/* function showMessage(text) // аргумент который передают в функцию)
{  
    alert(text);//то что функция делает
    let num = 20;

  } 

  showMessage("helo"); // вызов функции - сюда будет вписано то, что выводиться в аргументе */



 /*  function calc(a,b) {
    return (a+b);
  }
  console.log(calc(3,4));
  console.log(calc(23,655));

  function retVar() {
    let num = 50;
    return num;
  }

  let anotherNum = retVar();
  console.log (anotherNum); */


  let calc = (a,b) => a+b;
  console.log(calc(3,557));
  console.log(calc(23,655));